import 'package:flutter/material.dart';

class SecondHalf extends StatelessWidget {
  String result;
  SecondHalf({this.result});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text(
        result,
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}

class SecondHalflength extends StatelessWidget {
  String result;
  SecondHalflength({this.result});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text(
        result,
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}

class SecondHalfWeight extends StatelessWidget {
  String result;
  SecondHalfWeight({this.result});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text(
        result,
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}
